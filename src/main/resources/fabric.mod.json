{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "Artillery system with ballistic solver and recon camera.",
  "license": "All Rights Reserved",
  "environment": "*",
  "entrypoints": {
    "main": ["com.example.artillery.ArtilleryMod"],
    "client": ["com.example.artillery.client.ClientInit"]
  },
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}",
    "fabric-api": "*"
  }
}
